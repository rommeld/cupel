//! Code indexing for cupel - **placeholder, second iteration**.
//!
//! This crate is intentionally empty. The design it will implement is
//! written up in `CONCEPT.md` next to this crate's `Cargo.toml`.
//!
//! The one thing already decided: this crate will implement the
//! `CodeSearch` trait defined in `cupel-coding-agent::search`, so the
//! coding agent can swap its grep backend for an index backend without any
//! changes to the tool layer. That trait is the seam between iterations.
